"""Adapters that read data produced by the sibling `pdf-forensics-benchmark` project.

Coupled only to that project's output *file format* (its `labels.csv` and
`hashes.csv` column names), never to its Python package — the two projects
stay independent.
"""
